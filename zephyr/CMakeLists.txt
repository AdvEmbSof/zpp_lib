if(CONFIG_USE_ZPP_LIB)
  zephyr_include_directories(../)
if(CONFIG_USERSPACE)
  FILE(GLOB app_sources ../zpp_rtos/*.cpp ../zpp_drivers/*.cpp ../syscalls/time_syscalls.c)
else()
  FILE(GLOB app_sources ../zpp_rtos/*.cpp ../zpp_drivers/*.cpp)
endif()
  zephyr_library_sources(${app_sources})
if(CONFIG_USERSPACE)
  list(APPEND SYSCALL_INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR}/../syscalls)
  set(SYSCALL_INCLUDE_DIRS ${SYSCALL_INCLUDE_DIRS} PARENT_SCOPE)
endif()
endif()